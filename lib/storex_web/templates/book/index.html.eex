<%= for book <- @books do %>
  <div class="row">
    <div class="small-2 columns"><%= book.title %></div>
    <div class="small-2 columns"><%= book.image_url %></div>
    <div class="small-2 columns"><%= book.price %></div>
  </div>
<% end %>
